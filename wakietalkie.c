//at the first time of the round make ps_catcher =0;
 int ps_catcher[ps];
int walkieTalkie (int  i,int j)
{
	
	float dtc=(float)sqrt( pow(i-criminal_x,2) + pow(j-criminal_,2) );
	if (dtc<=2){
		ps_catcher[matrix[i][j]]=matrix[i][j];
		}
	if(ps_catcher[matrix[i][j]]==matrix[i][j])
		{
		if(dtc=(float)sqrt(2)){
			return 1;}
		
			else{
			return 2;}}
	else 	
		{return 0;}
			
}
switch(walkieTakie){
	case 1: \\ its time to catch;
	break;
	case 2: \\ just need to get a little closer;
	break;
	case 0: \\random moving;
	break;
}	